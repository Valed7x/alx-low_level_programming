#include "main.h"

/**
 * add - adds two integers and returns the result
 * @x: int to be added to b
 * @z: int to be added to a
 * Return: sum of a and b
 */
int add(int x, int z)
{
	int w;

	w = x + z;

	return (w);
}
