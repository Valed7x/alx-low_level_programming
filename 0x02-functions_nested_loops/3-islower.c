#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c: the character to check
 *
 * Returns 1 if c is lowercase
 * Returns 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
