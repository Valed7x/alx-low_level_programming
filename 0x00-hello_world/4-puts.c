#include <stdio.h>

/**
 * main - print the phrase "programming is like building a multilingual puzzle
 * Return: 0 if exited ptoperly, non-zero otherwise
 */
int main(void)
{
	puts("/"programming is like bulding a multilingual puzzle");
	return (0);
}
